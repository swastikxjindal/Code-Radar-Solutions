#include <stdio.h>

void function1(){
    int deciNumber;
    scanf("%d",deciNumber);

    
    int octalNUM[100];
    int i = 0;
    while (n!=0){
        octalNUM[i] = i % 8;
        n = n / 8;
        i++;

    }
    for (j = i-1;j>=0;j--)
    printf("Hexadecimal: %X",deciNumber);
    printf("Octal: %d",octalNUM[j]);

}